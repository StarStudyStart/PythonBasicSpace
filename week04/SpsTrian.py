#SksTaian.py
import turtle
def triangleDraw(len,x=0,y=0):
	turtle.goto(x,y)
	turtle.fd(len)
	turtle.right(120)
	turtle.fd(len)
	turtle.left(120)
	turtle.fd(len)
def SpsDraw(len,level):
	if  level == 0:
		triangleDraw(len)
	else :
		for 
triangleDraw(400)